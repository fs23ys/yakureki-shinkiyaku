# 薬歴新規薬コピペ Webアプリ 仕様書 v1.0

## 0. この仕様書の位置づけ

姉妹アプリ「**薬歴テンプレコピー**」（Googleドキュメントの薬歴SOAPテンプレートを取り込み、見出し単位で検索・プレビュー・コピーする静的Webアプリ）と、
**基本の仕組み（取り込み・解析・検索・プレビュー・コピー・保存）は同じ**。
異なるのは「見出しの並べ方・見せ方」のみ：元ドキュメントは薬効分類（カテゴリ）順だが、このアプリでは**薬品名のあかさたな（五十音）順**に並べ替えて表示する。

Claude Codeでの実装を想定している。Google Apps Script、Google Docs API、OAuthのいずれも使わない、完全に独立した静的Webアプリとして作る。

### 確認済みの前提

| 項目 | 決定事項 |
|---|---|
| 実装の土台 | 姉妹アプリ「薬歴テンプレコピー」と同じ静的Webアプリ方式（HTML/CSS/JS、サーバー不要） |
| データの原本（マスター） | 常にGoogleドキュメント側。アプリ内での直接編集はしない |
| データの取り込み方法 | Googleドキュメントを「ファイル→ダウンロード→ウェブページ(HTML)」で書き出し、そのファイルをアプリに取り込む |
| 見出しの並び順 | 元ドキュメントの薬効分類順（文書順）ではなく、**薬品名のあかさたな順**に並べ替えて一覧表示する（詳細は3章） |
| 見出しクリック時の動作 | クリックするとその場で内容プレビューが開く（Googleドキュメントへの遷移は不要） |
| コピー動作 | プレビュー内の「コピー」ボタンを押したときだけコピーする |
| データの保存先 | ブラウザ内（localStorage）に保存。次回開いたときも保持される |
| サンプルデータ | このフォルダの `薬歴新規薬.html`（実際のGoogleドキュメントエクスポート、見出し175件）を検証に使う |

---

## 1. 目的・背景

- 「新規に薬が処方されたとき」に使う薬品別の説明文（SOAP形式：`##S##` `##O##` `##A##` `##EP##` `##OP##`）のテンプレート集を、Googleドキュメントで管理している。
- 元のGoogleドキュメントは、**薬効分類（例：No.4 鎮痛薬、No.5 片頭痛治療薬…）ごとに薬品名がぶら下がる構成**になっている。
- しかし実際の使い方では、「薬効分類は覚えていないが薬品名は分かっている（または処方箋を見ればすぐ分かる）」ことが多いため、**薬品名をあかさたな順に並べたほうが探しやすい**。
- そのため、姉妹アプリ「薬歴テンプレコピー」と同じ土台の上に、**表示順だけを薬品名の五十音順に変えた別バージョン**を作る。

---

## 2. スコープ

- 対象は1つのテンプレート集（Googleドキュメント1つ分の内容、サンプル: `薬歴新規薬.html`）。
- Googleドキュメントへのライブ接続（Apps Script／Docs API／OAuth）は行わない。
- データの取り込みは、Googleドキュメントを手動でHTMLエクスポートしたファイルをアプリに読み込ませる方式。
- 姉妹アプリ「薬歴テンプレコピー」にある以下の機能は、そのまま流用する（再実装ではなく移植でよい）：
  - HTML解析ロジック（`##S##`〜`##OP##`ブロック抽出、`applyEffectiveBlocks`による本文なし見出しの補完）
  - 検索・更新エリア・プレビュー・コピー ボタン・localStorage保存
  - ダークモード、レスポンシブ（モバイル対応）
- 今回のスコープでの**新規要素**は「3. 見出しの並べ替え（薬品名の五十音順表示）」のみ。

---

## 3. 見出しの並べ替え（本アプリの核となる差分）

### 3.1 元データの構造（サンプル `薬歴新規薬.html` で確認済み）

- `H1`：薬効分類（例：`🔵 No.4 鎮痛薬`）。全175見出し中34件。
- `H2`：分類直下の薬品名、または「シナリオ区分」（例：`① 新規`, `② 新規（胃弱者向け）`, `③ 新規（頓服）`）のどちらかが入る。
- `H3`：シナリオ区分の下にある薬品名（例：`A. ロキソプロフェン`）。ほとんどが `英大文字 + ". " + 薬品名` という形式。
- `H4`：薬品名の下にあるさらに細かい用途分岐（例：`（１）痛み`, `（２）しびれ`）。

つまり「これは薬品名の見出しである」という判定は **見出しレベル（H1〜H6）では決まらず**、**タイトルの先頭が `A. ` `B. ` `C. `…（英大文字+ピリオド+スペース）で始まるかどうか**、または分類直下に直接ぶら下がる薬品名（シナリオ区分を挟まない場合）で判定する必要がある。
実装時は、サンプルファイルを使って「薬品名見出しの判定ロジック」を先に検証すること（姉妹アプリの手順1と同じ進め方）。

### 3.2 同じ薬品名が複数回登場するケース

同一の薬品名が、**異なるシナリオ区分の下に複数回**登場することがある（サンプルで確認済み：`ロキソプロフェン`が`①新規`/`②新規（胃弱者向け）`/`③新規（頓服）`の3か所に、`セレコキシブ`も同様に3か所）。

**方針**：薬品名ごとに1つの見出しエントリとしてまとめ、複数の文脈がある場合は、姉妹アプリの「本文が無い見出しの配下ブロックをそれぞれ分けて表示する」既存ロジック（`applyEffectiveBlocks`、パンくず形式のタイトル表示、個別コピーボタン）と同じ考え方で、**文脈ラベル（①新規／②胃弱者向け／③頓服など）付きで分けて表示・個別コピー**できるようにする。

### 3.3 並び替えロジック

1. 各「薬品名見出し」から、先頭の `A. ` `B. ` … のような記号・番号プレフィックスを取り除き、「素の薬品名」を得る。
2. 素の薬品名を **日本語の五十音順（あかさたな順）** でソートする。
   - JavaScriptの `localeCompare(b, 'ja')` （または `Intl.Collator('ja')`）を使うこと。単純な文字コード順（`sort()`のデフォルト）は、濁音・半濁音・長音（ー）・小書き文字などで五十音順とズレる場合があるため避ける。
   - サンプルの薬品名はほぼ全てカタカナ表記（エスゾピクロン、ロキソプロフェンなど）だが、一部カタカナ＋英数字混在（例：`ピコスルファートNa錠2.5mg`）もあるため、その場合の並び順は実装時にサンプルデータで目視確認する。
3. ソート後、**元の薬効分類（H1のタイトル）は捨てずに、各エントリの補足情報（例：小さいラベルや詳細パネル内の一言）として残す**とよい（「どの分類の薬か」も薬歴記載時に有用な情報のため）。ただし一覧のグルーピング・並び順には使わない。

### 3.4 見出し一覧のグルーピング表示

姉妹アプリのようなH1単位のアコーディオン（カード化）ではなく、以下のいずれかの形にする：

- **五十音の頭文字（あ行・か行…）でのセクション区切り**をつけて一覧表示する、
- または区切りなしで薬品名だけをフラットに五十音順リスト表示する。

どちらが使いやすいかは実装時に両方試してよい（姉妹アプリのように、UIの大枠が決まる提案は先に選択肢を示してから実装を進めること）。

---

## 4. 画面仕様

姉妹アプリ「薬歴テンプレコピー」と同様、以下を基本形とする：

- 上部（sticky固定）に、検索欄（🔍アイコン付き、薬品名の部分一致でリアルタイムフィルタ）・更新エリア（HTMLファイルの取り込み）・ステータスメッセージ（コピー成功時は✅付きのバッジ表示など）を配置。
- 左ペインに薬品名の五十音順一覧（3.4節の形式）、右ペインに選択した薬品の内容（マスター・ディテール形式）を表示する2カラムレイアウト（狭い画面では縦積み）。
- 内容表示エリアでは、`##S##` `##O##` `##A##` `##EP##` `##OP##` の各マーカーを色分けしたバッジ表示にする（姉妹アプリと同じ配色: S=青、O=黄、A=赤、EP=緑、OP=紫、いずれも白文字）。
- 「コピー」ボタンはクリック時に1秒間だけ緑色＋「コピーしました!✅」表示に変わる視覚フィードバックを入れる（姉妹アプリと同じ）。

---

## 5. 機能仕様

### 5.1 HTMLファイルの取り込み・解析

姉妹アプリの `js/parser.js` をベースに移植する。`##S##`〜`##OP##`ブロック抽出ロジックはそのまま使えるはずだが、サンプル `薬歴新規薬.html` で必ず動作検証すること（##S##の重複混入なし、など既存のチェック項目を踏襲）。

### 5.2 薬品名の抽出・並べ替え

3章の方針に従い、解析結果（見出し配列）から「薬品名見出し」を判定し、五十音順の一覧データを構築する。

### 5.3 データの保存・共有

姉妹アプリと同様、GitHub Pagesでの公開を前提とする：

- リポジトリに `data/template.html` として実データを同梱し、アプリ起動時に自動取得（fetch）する（手動アップロードは不要にし、全端末で同じ内容を見られるようにする）。
- 取得に失敗した場合（オフライン時など）は、ブラウザに保存済みのデータにフォールバックする。
- GitHub Pagesへのデプロイは、レガシー（Jekyll）方式ではなく **GitHub Actions方式**（`actions/upload-pages-artifact` + `actions/deploy-pages`）を使うこと。レガシー方式は大きめのHTMLファイルを含む構成でビルドがタイムアウトしやすいことが姉妹アプリの開発で確認済み。
- リポジトリ直下に `.nojekyll` を置くこと。

### 5.4 検索・プレビュー・コピー

姉妹アプリと同じ挙動（部分一致リアルタイム検索、クリックで詳細パネル表示、コピー ボタン）を踏襲する。

---

## 6. 運用フロー（テンプレートの追加・編集）

姉妹アプリと同様：

1. テンプレートを追加・修正したいときは、必ずGoogleドキュメント側で編集する。
2. 編集が終わったら「ファイル」→「ダウンロード」→「ウェブページ(.html)」でエクスポートする。
3. **ダウンロードされたファイル名は毎回`index.html`になるため、必ずリネームしてから**（例：`薬歴新規薬_最新.html`）プロジェクトフォルダに置く（アプリ本体の`index.html`を誤って上書きする事故を防ぐため）。
4. リポジトリの `data/template.html` を新しい内容に置き換えてpushする。

---

## 7. 非機能要件

- サーバー不要で動作すること。
- 見出し一覧・ボタンは視認性・クリックしやすさを優先する。
- デザインは姉妹アプリと同系統（パステルピンク・パープル系、角丸、Noto Sans JP）を踏襲してよい。踏襲せず独自にしたい場合は、着手前に方向性を確認する。
- エラー時（HTML解析失敗、コピー失敗など）はそれぞれ分かりやすいメッセージを表示する。
- スマートフォンでも一覧が使える程度の柔軟なレイアウトが望ましい。

---

## 8. ファイル構成（想定）

- `index.html` / `css/style.css` / `js/parser.js` / `js/app.js` — アプリ本体
- `data/template.html` — 共有テンプレート（GitHub Pages公開時に自動取得される実データ）
- `薬歴新規薬.html` — 検証用サンプル（このフォルダに同梱済み、175見出し）
- `test/` — Node + Playwrightによる自動検証（姉妹アプリと同様の構成を推奨）

---

## 9. 実装の進め方（推奨）

姉妹アプリの開発時と同じ、以下の順番を推奨する：

1. まずこの仕様書と `薬歴新規薬.html` を読み、不明点があれば先に確認する。
2. 「薬品名見出しの判定ロジック」と「五十音順ソート」を、サンプルファイルを使ってNode.jsで自動検証できる形で先に作る（3.1〜3.3節）。
3. 検索欄・見出し一覧（五十音順）・詳細パネル・コピー ボタンのUIを実装し、Playwrightでブラウザ実操作の検証を行う。
4. localStorageへの保存・復元を実装する。
5. GitHub Pagesへの公開（GitHub Actions方式、`.nojekyll`、`data/template.html`自動取得）を行う。

---

## 10. 今後の拡張候補（本バージョンのスコープ外）

- 薬効分類順表示と五十音順表示を切り替えられるようにする（タブなど）。
- 姉妹アプリ「薬歴テンプレコピー」とデータ取り込みUIを共通化する。
